#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define range std::ranges::iota_view

const char nl = '\n';
const int MOD = 1e9 + 7;

void fn() {}

int main() {
    // cout << fixed << setprecision(10);
    cin.tie(nullptr)->sync_with_stdio(false);
    int tt; cin >> tt;
    while (tt--)
        { fn(); }
}
